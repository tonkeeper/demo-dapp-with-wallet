import {TonConnectButton} from "@tonconnect/ui-react";
import './header.scss';

export const Header = () => {
    return <header>
        <span>TonConnect React reference</span>
        <TonConnectButton />
    </header>
}
